#
# Main playbook for the client configuration
#

- hosts: localhost
  become: yes
  become_user: qwattash
  gather_facts: yes

  vars:
    user_home: "/home/{{ ansible_facts['user_id'] }}"
    conf_user_local: "{{ user_home }}/.config/ansible_managed"
    conf_git_user_email: mzz.lrd@gmail.com
    conf_git_user_id: "{{ ansible_facts['user_id'] }}"

  roles:
    - shell
    - editor
    - terminal
    - vcs
    - wmanager
