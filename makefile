#
# This is a sample makefile that should be put in a project
# using anrem as its build system.
# It is assumed that ANREM resides in a directory named "anrem" in
# the root path of the host project.
#

ANREM_BASE := ./anrem

include anrem/makefile
