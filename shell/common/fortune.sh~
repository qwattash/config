#
# This shows the fortune message in a random color among the 8
# standard possible colors (except background)
#

source ~/git/config/term/colors.sh

function build_fortune_widget() {
    out_text=$(fortune)
    echo -ne "${Red}${out_text}"
}
